#install the pip virtualenv
>> pip install virtualenv   

#create a environment
>> virtualenv env     

#setting the ExecutionPolicy unrestricted to use any scripts in the editor but this could lead to securtiy 
flaw in the system
>> Set-ExecutionPolicy unrestricted 

#activate the env
>> .\env\Scripts\activate.ps1

#install pip flask in the env not in the main python
>> pip install flask